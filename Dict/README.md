# Dictionary scripts
These scripts are very rudimentary but ``update_dictionary.sh`` should download the latest dictionary from the kaikki website, run ``parse_dict.py`` on it, then copy it to the Resources folder to be used by the program.

This script is not necessary to be run as the git already includes the parsed dictionary
